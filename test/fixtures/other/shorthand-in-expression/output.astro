{enabled && <Header {content} />}

{enabled && <Header hello={hello} {content} />}

{
  [].map((item) => {
    return (
      <>
        <Header hello={hello} {content} />
        <Header {content} />
      </>
    );
  })
}
