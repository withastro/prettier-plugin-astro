---
'prettier-plugin-astro': patch
---

Fixes children hugging the closing bracket when an element's attributes wrap, and lets a lone long attribute wrap, with `astroCompressHTML: 'html'` or `'none'`
