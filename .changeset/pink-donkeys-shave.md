---
'prettier-plugin-astro': minor
---

Adds an `astroCompressHTML` option so formatting matches what Astro does to whitespace. Formatting changes throughout, and doctype identifiers and `<slot>` fallback content are no longer dropped.
